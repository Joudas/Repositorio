# Error
Uncaught Error: Attempting to use a disconnected port object
    at handleMessageFromPage

# Crear en el frontend un dashboard
Crear un dashboard donde aparezcan los board que tengo disponibles con un boton de crear board al lado
El encabezado debe ser el mismo que se usa en board, si hay que sacarlo de esa carpeta y ponerlo fuera para seguir el principio de bullet shot se hace, el color de fondo debe ser de gray-6
Se pueden traer los board por usuario, crear un api si no hay disponibles
Documentar el proceso, endpoints a usar, componentes a crear, etc

# Organizar las carpetas de components en board
Con el fin de mantener la arquitecruta limpia en las carpetas organizar los componentes por layout, todo, Inbox, card, etc